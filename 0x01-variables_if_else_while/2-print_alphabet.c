#include <stdio.h>

/**
 * Print out alphabets
 * Program prints out alphabets
 * Return:Always (Success)
 *
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar("\n");
	return (0);
}
