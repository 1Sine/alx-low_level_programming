#include "main.h"
/**
 * puts_half - Prints half of a string
 * @str: string to print
 * Return: length
 */
void puts_half(char *str)
{
	int longi = 0;

	while (*s != '\0')
	{
		longi++;
		s++;
	}

	return (longi);
}
